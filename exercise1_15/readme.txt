https://hub.docker.com/repository/docker/mst2343/curler/general

Command line clientURL

This project is used to test publishing Dockerfiles.

It's based on ubuntu 20.04 and curl. It runs curl for address given as first parameter 

Project is used by (for example.com)
docker run curler example.com 
